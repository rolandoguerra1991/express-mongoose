const authenticated = require('./authenticated');
const checkRole = require('./checkRole');
const checkAbility = require('./checkAbility');

module.exports = {
  authenticated,
  checkRole,
  checkAbility,
};
